<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PresenterApp.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:PresenterApp.Models"
    xmlns:vm="clr-namespace:PresenterApp.ViewModels"
    Title="{Binding Title}"
    x:DataType="vm:HomeViewModel">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            Grid.Column="0"
            Padding="0"
            Stroke="LightGray"
            StrokeThickness="1">
            <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,*">

                <Button
                    Grid.Row="0"
                    Margin="10"
                    Command="{Binding GoToManagementCommand}"
                    Text="Thêm/Sửa Sách, Loại sách, Tags" />

                <Label
                    Grid.Row="1"
                    Padding="10,10,10,0"
                    FontAttributes="Bold"
                    FontSize="Title"
                    Text="Lọc và tìm kiếm nội dung" />

                <VerticalStackLayout
                    Grid.Row="2"
                    Padding="10,5,10,0"
                    Spacing="5">
                    <Picker
                        Title="Lọc theo Loại Sách"
                        ItemDisplayBinding="{Binding Name}"
                        ItemsSource="{Binding FilterBookTypes}"
                        SelectedItem="{Binding SelectedBookType}" />
                    <Picker
                        Title="Lọc theo Sách"
                        ItemDisplayBinding="{Binding Name}"
                        ItemsSource="{Binding FilterBooks}"
                        SelectedItem="{Binding SelectedBook}" />
                    <Picker
                        Title="Lọc theo Thuộc tính"
                        ItemDisplayBinding="{Binding Name}"
                        ItemsSource="{Binding FilterAttributes}"
                        SelectedItem="{Binding SelectedAttribute}" />
                </VerticalStackLayout>

                <SearchBar
                    Grid.Row="3"
                    Margin="10,5,10,10"
                    Placeholder="Tìm kiếm trong thuộc tính đã lọc..."
                    SearchCommand="{Binding ExecuteSearchCommand}"
                    Text="{Binding SearchText}" />

                <BoxView
                    Grid.Row="4"
                    HeightRequest="1"
                    Color="LightGray" />

                <ActivityIndicator
                    Grid.Row="5"
                    HorizontalOptions="Center"
                    IsRunning="{Binding IsBusySearching}"
                    IsVisible="{Binding IsBusySearching}"
                    VerticalOptions="Center" />

                <ScrollView Grid.Row="5" IsVisible="{Binding IsBusySearching, Converter={StaticResource InvertedBoolConverter}}">
                    <CollectionView ItemsSource="{Binding SearchResults}" SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="vm:BookSummaryViewModel">
                                <VerticalStackLayout Padding="10" Spacing="5">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Label Grid.Column="0" VerticalOptions="Center">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span
                                                        FontAttributes="Bold"
                                                        FontSize="Large"
                                                        Text="{Binding Book.Name}" />
                                                    <Span Text=" (" />
                                                    <Span
                                                        FontAttributes="Italic"
                                                        Text="{Binding ResultCountText}"
                                                        TextColor="Gray" />
                                                    <Span Text=")" />
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                    </Grid>

                                    <VerticalStackLayout
                                        Margin="20,0,0,0"
                                        BindableLayout.ItemsSource="{Binding ContentEntries}"
                                        IsVisible="{Binding IsExpanded}">
                                        <BindableLayout.ItemTemplate>
                                            <DataTemplate x:DataType="vm:ContentEntryViewModel">
                                                <Frame
                                                    Margin="0,5"
                                                    Padding="10"
                                                    BorderColor="LightGray"
                                                    CornerRadius="5">
                                                    <VerticalStackLayout>
                                                        <Label
                                                            FontAttributes="Bold"
                                                            LineBreakMode="TailTruncation"
                                                            MaxLines="1"
                                                            Text="{Binding SummaryText1}" />
                                                        <Label
                                                            FontSize="Small"
                                                            LineBreakMode="TailTruncation"
                                                            MaxLines="1"
                                                            Text="{Binding SummaryText2}"
                                                            TextColor="Gray" />
                                                    </VerticalStackLayout>
                                                </Frame>
                                            </DataTemplate>
                                        </BindableLayout.ItemTemplate>
                                    </VerticalStackLayout>

                                    <BoxView HeightRequest="1" Color="{StaticResource Gray100}" />
                                </VerticalStackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </ScrollView>
            </Grid>
        </Border>

        <Grid Grid.Column="1" BackgroundColor="{StaticResource Gray100}">
            <Button
                HorizontalOptions="Center"
                Text="Chỉnh sửa slide"
                VerticalOptions="Center" />
        </Grid>

    </Grid>
</ContentPage>