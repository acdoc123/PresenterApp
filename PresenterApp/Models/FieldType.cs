// File: Models/FieldType.cs
namespace PresenterApp.Models
{
    public enum FieldType
    {
        Text,      // Tên (textfield)
        TextArea,  // Nội dung (textarea)
        Number,    // Số (chỉ số)
        Image,     // Hình ảnh
        Pdf        // File pdf
    }
}