<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PresenterApp.Views.HomePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:PresenterApp.Models"
    xmlns:vm="clr-namespace:PresenterApp.ViewModels"
    Title="{Binding Title}"
    x:DataType="vm:HomeViewModel">

    <Grid RowDefinitions="*, Auto">
        <CollectionView ItemsSource="{Binding Songs}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Song">
                    <Frame
                        Margin="10"
                        Padding="10"
                        BorderColor="LightGray"
                        CornerRadius="5">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:HomeViewModel}}, Path=GoToSongDetailCommand}" CommandParameter="{Binding}" />
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout>
                            <Label
                                FontAttributes="Bold"
                                FontSize="Large"
                                Text="{Binding Title}" />
                            <Label
                                FontSize="Small"
                                Text="{Binding Artist}"
                                TextColor="Gray" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button
            Grid.Row="1"
            Margin="20"
            Command="{Binding AddNewSongCommand}"
            Text="Thêm bài hát mới" />
    </Grid>
</ContentPage>